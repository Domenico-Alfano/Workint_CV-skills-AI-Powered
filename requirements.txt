# Stage A: DB foundation + ESCO loader.
SQLAlchemy>=2.0
psycopg2-binary>=2.9
pandas>=2.0
python-dotenv>=1.0

# Stage B: category -> ESCO classification (embeddings + lexical hybrid).
sentence-transformers>=3.0
rapidfuzz>=3.0

# API layer (backend for the frontend; Swagger UI at /docs is the test frontend).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # file uploads
requests>=2.31            # call the external CV extractor
